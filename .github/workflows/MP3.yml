name: M3U Listesini Guncelle

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Depoyu Çek
      uses: actions/checkout@v4

    - name: Python Kurulumu
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Gerekli Kütüphaneleri Yükle
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4

    - name: Scraper Scriptini Çalıştır
      run: python scraper.py

    - name: Değişiklikleri Kaydet ve GitHub'a Gönder
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # Müzik dosyamız oluştuysa depoya ekle
        if [ -f "müzikmp3.m3u" ]; then
          git add müzikmp3.m3u
          git diff --quiet && git diff --staged --quiet || (git commit -m "Otomatik Güncelleme: Müzik listesi yenilendi" && git push)
          echo "İşlem başarıyla tamamlandı!"
        else
          echo "UYARI: müzikmp3.m3u dosyası bulunamadı! Scraper çalışmadı veya link hatalı."
          exit 1
        fi
